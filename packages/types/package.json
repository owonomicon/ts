{
  "name": "@nomicon/types",
  "version": "0.0.2",
  "description": "collection of useful TypeScript types",
  "homepage": "https://github.com/owonomicon/ts/tree/main/packages/types",
  "repository": {
    "type": "git",
    "url": "https://github.com/owonomicon/ts"
  },
  "license": "MIT OR Apache-2.0",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "clean": "rm -rf ./dist",
    "test": "tsc --project tsconfig.test.json",
    "test:build": "tsc --project tsconfig.test-build.json",
    "release": "pnpm release:version && pnpm release:commit",
    "release:version": "pnpm version",
    "release:commit": "node ./scripts/commit-release.node.js"
  },
  "devDependencies": {
    "typescript": "^4.8.4"
  }
}
